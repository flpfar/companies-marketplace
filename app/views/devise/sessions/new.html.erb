<div class="absolute left-0 right-0 mt-4 text-center bg-red-200 text-sm z-10 mx-4">
  <p class="font-bold text-red-400">This is a demo version</p>
  <p>- Available only in Portuguese for now</p>
  <p>- Images not working (free server limitation)</p>
  <p>- Sign up with any email @coshop.com in order to test it</p>
</div>

<div class="login-container flex flex-col items-center justify-center h-screen relative">  
  <%= image_tag "logo-dark.png", class: " mb-2" %>
  <div class="login border bg-white border-gray-400 p-8 text-gray-900 rounded w-full max-w-md">
    <h2 class="text-center font-bold text-gray-900 text-lg">Entrar</h2>
    
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field mt-4">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'Email', class: "p-2 border border-gray-400 rounded w-full" %>
      </div>
    
      <div class="field mt-4">
        <%= f.label :password %><br />
        <%= f.password_field :password, autocomplete: "current-password", placeholder: 'Senha', class: "p-2 border border-gray-400 rounded w-full" %>
      </div>
    
      <% if devise_mapping.rememberable? %>
        <div class="field mt-4">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>
    
      <div class="actions mt-4">
        <%= f.submit "Entrar", class: "w-full py-2 bg-gray-900 hover:bg-gray-800 text-white cursor-pointer" %>
      </div>
    <% end %>
    
    <div class="devise-links text-center underline mt-4">
      <%= link_to "Criar uma conta", new_registration_path(resource_name), class: "hover:text-gray-600" %><br />
    </div>
  </div>

  <%= render 'shared/footer' %>

</div>
