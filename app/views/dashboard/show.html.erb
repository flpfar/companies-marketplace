<h2>Empresas</h2>
<ul>
  <% @companies.each do |company| %>
    <li>
      <%= link_to company.name, dashboard_company_path(company) %>
    </li>
  <% end %>
</ul>


<%= link_to "Adicionar empresa", new_dashboard_company_path %>