<%= form_with model: @sale_post, local: true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :category_id %>
  <%= f.collection_select :category_id, @categories, :id, :name, prompt: 'Escolha uma categoria' %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.label :price %>
  <%= f.text_field :price %>
  <%= f.submit 'Criar anúncio' %>
<% end %>